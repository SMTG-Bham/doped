doped.VASP_sets
=================================================

See the `VASP_sets`_ folder in the ``doped`` GitHub repository for the ``yaml`` configuration files with
the default ``INCAR``, ``KPOINTS`` and ``POTCAR`` settings.

.. _VASP_sets:
   https://github.com/SMTG-UCL/doped/tree/master/doped/VASP_sets

.. raw:: html
    <pre id="show-json-from-git"></pre>

    <script>
    var url = 'https://raw.githubusercontent.com/SMTG-UCL/doped/master/doped/VASP_sets/RelaxSet.yaml';
    fetch(url)
    .then(res => res.text())
    .then((out) => {
      document.getElementById("show-json-from-git").innerText = out
    })
    .catch(err => { throw err });
    </script>